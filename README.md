# Back-end IResolve

# Features
- Authentication (login, register)
- Needs (create)
- Users (read all, read, update)
# Requirements
- Java 17+
- Maven 3.9.9+
# Executar
```shell

mvn install
mvn spring-boot:run

```
